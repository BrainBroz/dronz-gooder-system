import{Router}from"express";import{requireAuth,requireStore}from"../../middlewares/auth";import*as c from"./purchase-orders.controller";export const purchaseOrdersRouter=Router();purchaseOrdersRouter.use(requireAuth,requireStore);purchaseOrdersRouter.get("/",c.list);purchaseOrdersRouter.get("/:id",c.get);purchaseOrdersRouter.post("/",c.create);purchaseOrdersRouter.patch("/:id",c.update);purchaseOrdersRouter.patch("/:id/status",c.status);purchaseOrdersRouter.delete("/:id",c.remove);purchaseOrdersRouter.post("/:id/items",c.addItem);purchaseOrdersRouter.patch("/:id/items/:itemId",c.updateItem);purchaseOrdersRouter.delete("/:id/items/:itemId",c.removeItem);
